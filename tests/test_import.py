from kirei_ui import (
    KireiAction,
    KireiActionGroup,
    KireiAlert,
    KireiBadge,
    KireiBreadcrumbs,
    KireiButton,
    KireiCard,
    KireiCheckbox,
    KireiColorDialog,
    KireiComboBox,
    KireiConfirm,
    KireiDateEdit,
    KireiDateTimeEdit,
    KireiDialog,
    KireiDivider,
    KireiDoubleSpinBox,
    KireiDrawer,
    KireiEmpty,
    KireiFileDialog,
    KireiFilterBar,
    KireiInput,
    KireiLabel,
    KireiList,
    KireiMenu,
    KireiMenuBar,
    KireiMessageBox,
    KireiNavItem,
    KireiPagination,
    KireiPassword,
    KireiPopover,
    KireiProgress,
    KireiRadio,
    KireiSearchBox,
    KireiSection,
    KireiShortcut,
    KireiSidebar,
    KireiSlider,
    KireiSpinBox,
    KireiSpinner,
    KireiStatusBar,
    KireiSwitch,
    KireiSystemTray,
    KireiTable,
    KireiTag,
    KireiText,
    KireiTextarea,
    KireiTimeEdit,
    KireiTitle,
    KireiToolbar,
    KireiTooltip,
    KireiTopBar,
    KireiTree,
)


def test_new_widget_exports() -> None:
    assert KireiAction is not None
    assert KireiActionGroup is not None
    assert KireiAlert is not None
    assert KireiBadge is not None
    assert KireiBreadcrumbs is not None
    assert KireiButton is not None
    assert KireiCard is not None
    assert KireiCheckbox is not None
    assert KireiColorDialog is not None
    assert KireiComboBox is not None
    assert KireiConfirm is not None
    assert KireiDateEdit is not None
    assert KireiDateTimeEdit is not None
    assert KireiDialog is not None
    assert KireiDivider is not None
    assert KireiDoubleSpinBox is not None
    assert KireiDrawer is not None
    assert KireiEmpty is not None
    assert KireiFileDialog is not None
    assert KireiFilterBar is not None
    assert KireiInput is not None
    assert KireiLabel is not None
    assert KireiList is not None
    assert KireiMenu is not None
    assert KireiMenuBar is not None
    assert KireiMessageBox is not None
    assert KireiNavItem is not None
    assert KireiPagination is not None
    assert KireiPassword is not None
    assert KireiPopover is not None
    assert KireiProgress is not None
    assert KireiRadio is not None
    assert KireiSearchBox is not None
    assert KireiSection is not None
    assert KireiShortcut is not None
    assert KireiSidebar is not None
    assert KireiSlider is not None
    assert KireiSpinner is not None
    assert KireiSpinBox is not None
    assert KireiStatusBar is not None
    assert KireiSwitch is not None
    assert KireiSystemTray is not None
    assert KireiTable is not None
    assert KireiTag is not None
    assert KireiText is not None
    assert KireiTextarea is not None
    assert KireiTimeEdit is not None
    assert KireiTitle is not None
    assert KireiToolbar is not None
    assert KireiTooltip is not None
    assert KireiTopBar is not None
    assert KireiTree is not None
